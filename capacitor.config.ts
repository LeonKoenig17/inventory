import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.leonkthedev.boxinventory',
  appName: 'Box Inventory',
  webDir: 'dist/inventory/browser'
};

export default config;
